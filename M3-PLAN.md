# M3 — MLS application messages in the app (Path A). Continuation plan.

> Started 2026-07-01. **Read this first when resuming.** M3 turns the host-verified + fuzz-closed MLS spike
> into a WORKING, parallel MLS message path. Sender-Keys stays live; migration Sender-Keys→MLS is **M5**,
> separate. Boundary: **wrap the verified spike, never rewrite it.**

## 🗺️ SESSION-HANDOFF MAP (updated 2026-08-05) — read this block first

**STATE: git clean, all committed. Rust 74/74 host, tsc clean, KAT byte-identical, both ABIs built.**
**🏁 THE HOST-SIDE M3 PLAN IS EXHAUSTED.** Every host item is closed (see ✅ below). What remains is
the ON-DEVICE SESSION (next block) + the deferred v2 races + backlog.

**✅ DONE this session** (each host-verified, committed):
- **Route-2 libcrux bump** (@73f9624) — openmls 0.9.0-rc.1 family; 4 HIGH advisories closed (cargo audit 0); host-KAT byte-identical.
- **MLS roles + delete-for-all** (@8c59b1f→7863278) — owner-signed genesis + hash-chained role log (reuses grouplog.js), tree-attested anchor (member_account_keys). mlsroles 29/29.
- **Leaf-key hardening** (@e2bbd5f→d8cf1ce) — PRIMARY uses a dedicated per-device MLS leaf (not the account key); ghost-defense unchanged. mlsleaf 10/10 + Rust PRIMARY-model anchor.
- **Single-use KeyPackage** (@73c93c7→89ff4d6) — pool of 10 one-time KPs + reusable last-resort; server pop (consumeMkp). server mls-kp-pool 12/12. (1:1 X3DH checked — healthy, oneTimeCount:32, NOT the same bug.)
- 🏁 **Tier-2 stateful fuzz** (@70e7736→588ef06) — **CLOSED 2026-08-07 (M3 tail retired).** 3 lanes ×
  **49h07m, 0 crashes**, no crash/leak/timeout artifact from any lane (the 14 files in artifacts/ are
  `slow-unit-*` — slow, not failing). **Closed on the 48h-crash-free half of the criterion only: the
  edge plateau was NOT reached.** The operator's decision was taken while `t2_msan` showed a full
  plateau (2000/2000); by the time closure ran, msan had found another edge (cov 7019→7020,
  flat 5/2000), and proc/ops were also still moving (0/2000 each). Recorded rather than smoothed:
  the safety result is **0 crashes over two days**, not saturation — a longer campaign could still
  surface something. Final stats + reasoning in `fuzz/TIER2-FINAL.md`; corpus (333MB, ~238k files)
  left in place, gitignored by design. **WSL dependency retired with it.**
- **Multidevice v1** (@3968910 1a, @fbb3545 1b-1, @8cf7d0b 1b-2) — a member's devices join at add-time; per-device fan-out + self-sync; LINKED KP-publish fixed. mlsFanout 26/26.
- **Device removal (v2)** (@4e014d4→0644243) — remove ONE device leaf (PCS) + add_revocation (re-add block) + member_devices FFI + GroupInfo UI. Rust 73/73, authz 30/30.
- **Late-join Stage A** (@ca906d6) — mlsLateJoinDevice: primary adds its own new device leaf into ONE existing group. Rust 74/74. No new FFI.
- **Late-join Stage B** (@75f9a71 tests+orchestrator, @3a9d081 wiring) — orchestration CLOSED: `pairing.primaryPairOverRelay` fires `notifyDeviceLinked(deviceId)` (pure registry `mlsLateJoin.js`, no pairing→messaging import) → `mlsLateJoinAllGroups` iterates `mlsRoster.listGroups()` SEQUENTIALLY through `mlsLateJoinDevice`; 1 retry per group with a pending-commit yield between attempts (Stage A reports failure as boolean → retry on ANY failure = safe superset, mechanism idempotent); partial failure isolated, `{ok, failed[reason]}` report + diag. mlsLateJoin host 24/24. Stage A + receive path untouched.
- **mid/reply/mentions on MLS text** (@699e646 tests+mirror, @ee9ca2b wiring) — MLS text now rides the SAME `{t:'text', body, mid, fwd?, reply?, mentions?}` envelope as Sender-Keys (`mlsText.buildTextEnvelope`, field-for-field mirror; fields INSIDE the encrypted app payload — no MLS extensions). Shared `mid` = the store mid space del-for-all/react already target → reply/react/del address MLS text identically on EVERY device (encrypt-once self-sync carries the same mid). `MLS_CTRL_TYPES` += 'text' → frozen shared `handleGroupControl` branch parses it (`handleMlsIncoming` untouched; raw non-0x01 plaintext from older spike builds still lands on the raw-text branch). Reply = snapshot → renders without the original (late-join/deleted). ChatScreen mentions read the MLS roster. mlsText host 25/25.
- **Dedicated linked MLS leaf** (@baf5a63 tests+formats, @f9329df wiring) — the LAST host item; v1 cross-protocol reuse (linked's 1:1 key doubled as the MLS leaf) REMOVED. At pairing the new device generates a SECOND random Ed25519 (MLS leaf); `{pub,sig,mpub,msig}` rides the sealed channel (per-key H6 transcript sig = PoP/anti-relay); the primary signs TWO certs, relays `{d,m}`; invalid mls cert → pairing fails closed, absent → v1 fallback (old primary/old device both compatible). Leaf secret never leaves the device (primary sees pubs only — host-asserted). `mlsLeaf.ts` holds the linked record (key+cert) + the PRIMARY-side directory `com.kvant.mlsleafdir` (1:1 deviceId → mls leaf cert-id): resolves the late-join idempotency gate (tree keys leaves by cert.device_id = fingerprint(mls pub) ≠ transport id) and lets `unlinkDevice` mint the SECOND (mls-leaf) revocation so a 1:1 unlink no longer leaves the leaf cert alive. mlsWiring prefers the dedicated record, v1 fallback for pre-existing pairings (greenfield, no migration). Rust: NO diff (separate-key model = what ghost-defense always verified). mlslinkedleaf host 32/32.

**📋 NEXT:**
1. **🟡 D5 (field remainder only, small)** — everything else is closed. Left: field unlink-PCS across `d3mls`/`d3mls2`/`d3mls3` (host-proven in D3 part 1, needs the device confirmation), re-pair + the optional camera-QR operator minute (JSON-paste used throughout the block, camera path unverified in the field), M2 spike-APK refresh (same `.so`, KAT + parity + bench per SoC). Use marker-addressed probes (D4 rule) for every group assertion. **simd128: DECIDED — keep portable** (D2 perf); reopen only on a profiling hotspot.
2. **v2 remainder**: late-join polling/offline-catchup (persistent pending-marker — primary offline at pairing → catch up on next launch), deep-race commit-queue coordinator.
3. **Backlog**: (a0) **SILENT-SUCCESS AUDIT** (from the D5 finding): sweep every pass that reports `ok=N/N` and verify it asserts the ACTUAL EFFECT, not just that the primitive returned true — `mlsEvictDeviceFromAllGroups` reported a perfect 3/3 while evicting nothing. Candidates to re-read with that lens: late-join pass, own-device join, pin push counts; (a) **событийный re-trigger** late-join по факту публикации KeyPackage девайса вместо таймеров — D2 показал ДВЕ независимые тайминговые зависимости (5s grace для KP-publish + 3s запас на обработку якорей; поле дало margin всего ~2.6s), событие схлопнет обе; (a2) **self-heal незавершённого join** (leaf в дереве, у устройства нет state — идемпотентный late-join честно отвечает «уже член» и welcome не переотправляет): авто leaf-remove+re-add, рассматривать в паре с (a); (a3) **unlink должен удалять leaf устройства из ВСЕХ MLS-групп** — сейчас отвязка чистит только 1:1-регистрацию и ревокации, leaf остаётся в дереве (PCS при отвязке НЕ достигается; примитив remove_device уже есть); (a4) в дереве накапливаются осиротевшие leaf'ы прошлых pairing-циклов — нужна чистка/дедуп + читаемые id в UI (сейчас укороченный hex, сопоставить с устройством трудно); (b) удалить raw-приём MLS-текста после девайс-сессии (валидация ненужности старых спайк-билдов); (c) **v1 pairing fallback sunset**: absent mls-cert → fail-closed перед продом (сейчас = даунгрейд-окно для old-build pairing; стрип-стойкость подтверждена — payload целиком в channelSeal AEAD, выборочный стрип mpub/msig невозможен без полного MITM канала = ловится SAS) + diag-видимость v1-fallback уже на pairing-этапе (сейчас diag только при MLS-init); (d) **simd128(NEON) libcrux-ml-kem НЕ включён** (portable = единственный путь, D1-факт) — решение об активации после D2-замера перфа; включение = НОВЫЙ кодоген-путь → обязательный повторный device-KAT на обоих SoC.

**🔴 ON-DEVICE SESSION — remaining gate. ✅ D1 CLOSED (2026-08-06): device-KAT PASS on BOTH SoC.**
- **✅ D1 — device-KAT PASS on BOTH devices (@cd03339 prep, @14189d6 extended runner; tag `m3-host-complete` = 29b7687):**
  - **SoC pair actually covered**: `N8AIOC1194109KB` = ASUS_AI2203_D — **ROG Phone 6D → MediaTek Dimensity 9000+ (MT6983W/CZA)**, Android 14 (plan said "Snapdragon" — the AI2203 is the Dimensity variant; serial matches the planned device) + `R58M31T663D` = SM-G975F S10+ — **Exynos 9820**, Android 15/Lineage. Two independent aarch64 implementations.
  - **Per-SoC result (IDENTICAL on both, byte-for-byte)**: ML-KEM-768 pk `c12e9e39…fbc707` / ss `78dbb52f…8f9322` == host PIN (deterministic ✓ roundtrip ✓ pinned ✓); ChaCha20-Poly1305 RFC 8439 §2.8.2 ct+tag exact (poly1305 path), round-trip ✓, advisory no-panic edges (short/tampered ct → typed error) ✓; Ed25519 RFC 8032 §7.1 T1+T3 pk/sig exact, verify ✓, tamper reject ✓. **All 4 HIGH-fix code paths device-verified, not just ML-KEM.**
  - **Artifacts run**: KAT runner (aarch64) sha256 `7a8dfbda…e4460d50` (extended vs prep-hash `242e754f…` — expected, ШАГ 1); production arm64 `libkvant_mls.so` `3120cbee…c5fa2dbe` UNCHANGED (lib sources untouched; runner links the same lib+locked crates). Toolchain: NDK 28.2.13676358, cargo-ndk 4.1.2 `-P 26`, aarch64 baseline target-features. **PATH FACT: libcrux-ml-kem simd128/NEON intrinsics NOT in the feature graph — portable = the sole compiled path in .so and runner; the run validates LLVM aarch64 codegen (incl. autovectorization) against the x86 PIN.**
- **✅ D2 — M3 device-test DONE (2026-08-06)**: all phases A–G PASS on the live trio (A = ROG6D primary, B = S10+ linked, C = emulator peer, real VPS relay). **9 field bugs found, every one fixed with a host-test repro first** (details below). Rust untouched (74/74), host-KAT byte-identical, crypto parity 18/18.
  - **Phase matrix**: A infra PASS · B MLS baseline A↔C (group/text/mid/reply/mentions/del-for-all by wire mid/KP discipline `mkp#0`→`mkp#1`) PASS · C pairing v2 (QR payload decoded from a screenshot and pasted — same crypto path, no camera; SAS matched every run; dual-cert {d,m}; dedicated leaf; addDevice accepted) PASS · D late-join Stage B (auto-trigger, ok=1/1 in 3 runs) PASS · E multidevice fan-out (C→A+B independently; A→C+B **identical frame**, encrypt-once self-sync; del-for-all from C tombstones on A **and** B) PASS · F removal v2 (unlink → **2 revocations**: 1:1-id + mls-leaf-id, server tombstone; per-device leaf-remove commit applied everywhere; **PCS PROVEN**: removed device gets the frame but fails `GroupStateError(UseAfterEviction)`) PASS · G perf PASS.
  - **Field timings (Stage B)**: pairing→KP-publish ≈2.4s; pairing→pass-end ≈6.1s (5s grace); pin push #1 fails (`no bundle`, device hasn't published yet), push #2 after the grace succeeds; anchors applied ≈7.4s, welcome ≈10.5s. Two independent timing dependencies now — see backlog (a).
  - **Perf (median of 200, release, portable ML-KEM-768)**: Dimensity 9000+ keygen 112µs / encaps 78µs / decaps 62µs / **handshake 252µs**; Exynos 9820 82 / 76 / 51 / **209µs**. → **simd128/NEON NOT worth enabling**: the win is tens of µs against a new codegen path plus a mandatory device-KAT repeat. Decision: keep portable.
  - **9 field bugs** (test → fix commits): open-once sealed frame + deviceId from payload (`a842a5d`/`67cd66a`) · linked cert persisted decoded, server refused addDevice (`3701662`/`60a74d1`) · getMlsIdentity read account before linked → MLS never initialised on linked (`3701662`/`60a74d1`) · **no trust-anchor source on linked → every welcome fail-closed** (sanctioned pin-sync, `7569db6`/`7172a3a`) · **devicecert used Node `Buffer` → own device pruned as ghost, all revocation paths dead on Hermes** (`04057a9`/`a3b78d8`) · pin push before the device published its bundle (`395ce91`/`9440c87`) · arrival order ≠ completion order on the receiver (`ceb6f31`/`d0984d3`) · pin backfill missing on launch (`af10b90`) · raw i18n keys in the device dialog (`46a6e9e`).
  - **Server rollout**: the M3 server code was NEVER deployed (the old "ZERO server change" note was wrong against the whitelist-style deployed `getBundle`). Deployed this session: `msgserver.js`+`db.js` @c600612, backup `/opt/kvant/backup-d2-20260806-005929/`, `DEPLOYED_COMMIT` written on the VPS. Rollback = restore the two files + restart (the new sqlite table is ignored by the old code).
  - 🔴 **PROCESS RULE for every future device session**: inventory MUST diff `DEPLOYED_COMMIT` on the VPS against the repo before any device work — this whole class (a fully host-green feature dead in the field) came from a silent deployment gap.
- **🟡 D3 — PARTIAL (2026-08-06)**: host-parts 1–3 CLOSED + committed; field verification INCOMPLETE (session context ran out mid-part-4). Nothing uncommitted; tree clean.
  - **Part 1 — unlink ⇒ PCS: DONE** (@5133ff3 tests / @c0db71a impl). `mlsRemoveDeviceLeaf` splits the leaf-remove half out of `mlsRemoveDevice` (idempotent: absent leaf = no-op, no epoch churn) so `mlsEvictDeviceFromAllGroups` sweeps every group while unlink mints exactly ONE revocation; wired into LinkDeviceScreen after the revocation broadcast; leaf id resolved via the pairing directory. mlsUnlinkPcs 18/18 (all groups evicted, epoch rotates per removal, partial failure isolated **and asserted to be a real residual**, idempotent re-run, no-groups/absent-leaf no-ops).
  - **Part 2 — raw MLS-text receive branch: REMOVED** (@c0db71a impl / @16c1679 test). Raw (non-0x01) payloads now drop fail-closed like any unknown type. mlsText 30/30 with a permanent negative test (raw utf-8, JSON-looking raw bytes, empty payload → drop; envelope text still dispatches). **RECEIVE-PATH FREEZE v3** = `handleDeliver`/`handleMlsIncoming` incl. the `t:'pins'` branch, MINUS the raw branch.
  - **Part 3 — coverage gap: MECHANISM WAS MISSING, fixed** (@97f96c9 tests / @a094d55 impl). `mlsCreateGroup` seeded the roster with `me` alone and never joined the account's own linked leaves — late-join only ever ran from the pairing hook, so a group created AFTER pairing silently excluded the paired device (exactly why D2 never caught it: every membership there came via the pairing path). Fix reuses the late-join primitive over `descriptorsOf(me)`. The peer direction was already correct (`mlsAddPeer` adds every device of the peer) and is now pinned against regression. mlsOwnDevices 11/11.
  - **Part 4 — field: PARTIAL.** ✅ APK built + installed on all three nodes (`.so` inside byte-identical to D2: `d5b417eb…`; APK arm64 `529626ce…`, x86_64 `fdb69d93…`); ✅ **scenario 3a PASS on hardware** — A created `d3mls2` with B already linked → `mls: own-device join → ok=1/1`, B logged `joined group … via welcome`, and `d3mls2` shows TWO d3alice leaves in the device dialog; ✅ i18n fix confirmed on device (was `GROUP.REMOVEDEVICE`, now «УДАЛИТЬ УСТРОЙСТВО»). ❌ NOT DONE: scenario 3b (external creator with a multidevice member), field unlink-PCS across all groups, re-pair + camera-QR moment, spike-APK refresh. 🔴 Also OBSERVED, needs its own diagnosis next session: after joining `d3mls2`, B answered `UseAfterEviction` to later group traffic — the leaf is in the tree per A's device dialog, so this is either a welcome/commit ordering race during create+add-member, or the probe went to the old `d3mls` (both groups render as «Group» on C and were selected positionally). **Do not treat 3a as fully field-proven until this is resolved.**
- **✅ D4 (2026-08-06) — the D3 anomaly is CLOSED: it was INSTRUMENTATION, not protocol.**
  - **Verdict with proof**: probes had been selected POSITIONALLY on the peer, where every MLS group renders as the generic "Group" (the name never travels in the welcome). Marker-addressed re-run: `MARKERALPHA` → `d3mls` → B answers `UseAfterEviction` (**correct** — B was evicted from that group by the D2 unlink tests); `MARKERBETA` → `d3mls2` → B **received and rendered** it. So in D3 both peer probes had landed in the OLD group. No welcome/commit race exists; **scenario 3a stands as genuinely PASS**, and no protocol fix was needed.
  - **Harness rule pinned permanently** (@a000a9c, mlsGroupAddressing 10/10): every probe carries a unique marker and a group is identified by the marker read back, never by list position. This cost two diagnosis cycles (D2 phase B, D3 3a) — the same trap both times.
  - **✅ Scenario 3b PASS on hardware**: external creator C made `d3mls3` with `d3alice` → **BOTH** of alice's devices joined (A `joined group … via welcome from C`, B `joined group … via welcome from C`), each with its own welcome — `mlsAddPeer` adding every device of a multidevice member is now field-proven, not just host-tested.
  - ❌ **Still not done** (carry forward): field unlink-PCS across all three groups, re-pair + the camera-QR operator moment (all pairing runs in the whole block used JSON-paste — the camera path still has NO field coverage), M2 spike-APK refresh.
  - Tier-2 fuzz at D4: 23h02m/48h, edges still moving → criterion not met, campaign untouched.
- **🟡 D5 (2026-08-06) — field unlink-PCS run: FOUND A REAL RESIDUAL, fixed on the host; needs one confirmation run.**
  - **Start state pinned by markers** (D4 discipline): B was a healthy member of `d3mls2` (`MARKERGAMMA`) and `d3mls3` (`MARKERDELTA`) — both received and rendered on B; `d3mls` had B already evicted from D2.
  - 🔴 **FIELD BUG (order of operations)**: unlink reported `unlink eviction → ok=3/3`, but every group logged `not in … — nothing to evict` and `d3mls2` still showed **2 leaves** afterwards. Cause: `unlinkDevice` deleted the pairing-directory entry while minting the mls revocation, and the eviction pass runs AFTER unlink → `resolveLeafId` fell back to the transport id (a dedicated linked leaf is keyed by fingerprint(mls pub)), matched nothing, and the sweep became a silent no-op. The unlinked device was only cut off at the TRANSPORT level (server dropped its registration: `identify rejected: no such account/device`) — **that is not post-compromise security**.
  - ✅ **Fixed** (@9af804b repro / @85df24d fix): the directory entry survives unlink; the eviction pass drops it only after a fully successful sweep and keeps it when any group failed (so a recovery re-run can still resolve). mlsUnlinkPcs 24/24.
  - ⚠️ **Field re-confirmation still owed**: rebuild + install + repeat the unlink sweep and assert `leaf-remove` per group with marker probes. Also still owed: re-pair (+ optional camera-QR minute — the whole block ran JSON-paste) and the M2 spike-APK refresh.
  - Tier-2 fuzz at D5: 23h22m/48h, edges still moving → untouched; closing moves to the first session past 48h.
- **✅ D6 (2026-08-06) — unlink-PCS fix CONFIRMED IN THE FIELD.** Built at HEAD 0715bc5 (`.so` inside the APK byte-identical to the D2 reference `d5b417eb…`; APK arm64 `de2881c8…`, x86_64 `268f7bc1…`), installed on all three nodes, B wiped and re-paired.
  - **Re-pair (JSON-paste; camera-QR still unverified)**: SAS matched (`obscure sock pulp arrest laugh`), `pins: pushed 1 anchor(s) to 1 device(s)` after the grace-retry (attempt 1 → 0 devices, as structurally expected), B applied the anchors at 12:41:33, then **`late-join pass → ok=3/3`** and B logged `joined group` for all three groups at 12:41:36–37. Anchors-before-welcome ordering from D2 holds.
  - 🎯 **THE CONFIRMATION that D5 lacked — actual effect, not the pass report**: `d3mls2` had **3 leaves** before unlink (A-primary + fresh B + the orphan D5 left behind) and **2 leaves** after → the fresh B leaf `F01A70E7` is genuinely evicted. The diag now reads `unlink eviction for ‹3df22168e1a6› (leaf ‹f6edc5edfee4›)` — resolve returned a leaf id **different from the transport id** (in D5 they were identical, which was the tell), and there is **not a single `nothing to evict`** line. PCS on unlink is real now, not transport-only.
  - Note on the last assertion: once unlinked, the device is also dropped by the server (`identify rejected: no such account/device`), so no further frame reaches it — `UseAfterEviction` from inside is unobservable by construction. The eviction is proven by the tree composition + the resolve log instead.
  - ⚠️ Still owed (small): M2 spike-APK refresh; the orphan leaves from earlier cycles are still in the trees (backlog item); camera-QR path remains without field coverage.
  - Tier-2 fuzz at D6: 23h36m/48h, edges still moving → untouched.
- **✅ M2 SPIKE TAIL CLOSED (2026-08-06, A1 session).** Diagnostics runner rebuilt at HEAD 7cb678c: `kat` (aarch64) sha256 `0aeb95ac…8daa`, linking the SAME production lib — `libkvant_mls.so` arm64 `3120cbee…c5fa2dbe`, **byte-identical to the D1/D2 reference** (no Rust change since). Run on BOTH SoC, all vectors green per device (`KAT_PINNED_OK`, `AEAD_CT/TAG/EDGE_OK`, `ED25519_T1/T3_SIG_OK`, `DEVKAT_ALL_OK=true`); crypto-parity 18/18 verified on host (it is a host invariant over crypto-core↔vendored, not a per-device check). Bench (median of 200, portable ML-KEM-768): **Dimensity 9000+ 125/84/67µs, handshake 276µs** · **Exynos 9820 78/76/56µs, handshake 210µs** — same order as D2 (112/78/62→252 and 82/76/51→209; the Dimensity delta is thermal/background noise, not a regression). **All pre-Route-2 M2 spike APKs are hereby OBSOLETE** — they predate the libcrux bump and must not be used for any verification.
- **Tier-2 fuzz @D1-run (2026-08-06): RUNNING, criterion NOT met** — 3 lanes crash-free, ~8h04m elapsed (need 48h), edges still moving. Campaign untouched.

**🔴 HONEST RESIDUALS (documented, accepted):**
- Admin removing ANOTHER account's device = leaf-remove/PCS only, NO cert-revocation (admin lacks that account's key) → full re-add protection is SELF-only (main "seized device" case works).
- Self-sync commit RACE (two of my own devices committing at once) — v1 relies on the primary committing.
- Late-join races: best-effort 1 retry (deep commit-queue = v2).
- 1:1 X3DH prekey supply is HEALTHY (oneTimeCount:32) — verified, not a bug.

**🔴 INVARIANTS — do NOT touch:** the RECEIVE path (`handleDeliver`/`handleMlsIncoming` — per-device-agnostic by construction; **the sanctioned `ctrl.t === 'pins'` branch added in D2 is part of that freeze from now on**), `senderkey.js` + Sender-Keys `groupEncryptAndFanOut`, the audited `grouplog.js` primitive, the Rust ghost-defense/remove/add core. Keep KAT byte-identical. Keep Contract-2 FROZEN/FREE classification intact (openmls `extensions-draft`/`virtual-clients-draft` stay OFF — enabling adds unclassified StorageProvider methods). Every piece → host-test first.

## ✅ Done this session (Phase 0 contract + Phase 1 vertical slice, host-GREEN)

**`src/client.rs` — `MlsClient` (uniffi Object)** — the app-facing bridge. Every method delegates to a
verified spike function (inherits auditor + fuzz verification); orchestration only:

| FFI method | wraps (verified) | returns |
|---|---|---|
| `new(kek, dev_priv, dev_pub, identity_blob)` | KseProvider + SignatureKeyPair::from_raw + self-pin | `Arc<MlsClient>` |
| `pin_account(id, pub)` | TrustStore.pin | — |
| `make_key_package()` | KeyPackage::builder().build | KP wire |
| `create_group()` | MlsGroup::new (floor config) | group_id |
| `add_member(gid, kp)` | **guarded_add_members** (ghost-checked) + merge | `AddResult{commit, welcome, ratchet_tree}` |
| `join_from_welcome(welcome, tree)` | **dispatch_welcome** (whole-tree ghost + floor) under `guard()` | group_id |
| `encrypt_message(gid, pt)` | MlsGroup::create_message | app-msg wire |
| `process_incoming(gid, wire)` | **dispatch_group_message** (fail-closed) under `guard()` | `IncomingResult{kind, plaintext?}` |
| `export_state()` / `import_state(blob)` | KseStorageProvider export_sealed/import_sealed | sealed blob / — |

**`src/storage.rs`** — added `export_sealed()`/`import_sealed()` (Contract-2 ciphertext dump for app-managed
persistence, decision 2b). **`src/lib.rs`** — added `mod client;`.

**Host tests (`src/client/tests.rs`), 3 GREEN (full suite 60/60):**
- `app_message_roundtrip` — alice creates group → adds bob → bob joins via Welcome → "hello" alice→bob and "hi back" bob→alice both decrypt.
- `forged_keypackage_rejected` — a ghost KeyPackage (cert not chaining to the pinned account) → `add_member` returns a typed reject (ghost-defense fail-closed **through the wrapper**).
- `export_import_persistence` — alice exports sealed state → fresh alice2 (same KEK+device key+identity) imports → **continues the group** (encrypts, bob decrypts). Proves the survive-restart foundation.

**Verified on aarch64 cross-compile (clean).**

### ✅ Session 2 (2026-07-01 cont.) — remove + native bridge + KEK + file-persist
- **`remove_member(gid, member_account_id)` FFI** — resolves the account's leaf/leaves + `MlsGroup::remove_members` + merge (Remove introduces no new key → no ghost-guard needed, per auditor). Host test `remove_member_pcs_and_no_participation` GREEN: removed member **cannot read post-removal traffic (PCS)** and **cannot send** afterward. (full suite 61/61, aarch64 clean.)
- **Native bridge `KvantMlsModule.kt`** — regenerated UniFFI bindings (MlsClient Object + AddResult/IncomingResult/IncomingKind). Added m3 @ReactMethods: `m3Init`/`m3PinAccount`/`m3MakeKeyPackage`/`m3CreateGroup`/`m3AddMember`/`m3JoinFromWelcome`/`m3RemoveMember`/`m3EncryptMessage`/`m3ProcessIncoming` (base64 at the JS boundary). **Kotlin compiles (`compileReleaseKotlin` SUCCESSFUL).**
- **Stable KEK** — `stableKek()`: get-or-create a 32-byte MLS KEK wrapped by a non-exportable AndroidKeyStore AES-GCM key `com.kvant.mlskek`, wrapped blob in SharedPreferences. Stable across restarts, no extra gradle dep. *(TODO: add to KvantWipeModule emergency-wipe clear path.)*
- **File persistence (decision 2b)** — `persist()` writes `exportState()` (already Contract-2 sealed ciphertext) to `filesDir/mls-state.bin` after every mutation; `m3Init` reloads it. Survives restart. *(TODO: current persist rewrites the WHOLE state per op incl. every message — fine for foundation, optimize to incremental/debounced later.)*

### Confirmed design decisions (agreed with owner)
1. Stateful `uniffi::Object` (`MlsClient`), single `Mutex<Inner>` for Send+Sync.
2. Persistence = app-managed sealed blobs (`export_state`/`import_state`); Kotlin persists to a Keystore-encrypted file. Rust storage stays in-memory.
3. KEK = stable `com.kvant.mlskek` Keystore key (NOT random per-call).
4. Identity = the app's EXISTING device Ed25519 + C3-LINKED account cert (same as 1:1) → `identity_blob = encode_identity(account_id, cert)`. No new keys.
5. Wire routing tag `0x03 = MLS` on the same opaque transport as Sender-Keys.

### Design notes / gotchas
- Groups load-on-demand (`MlsGroup::load(storage, gid)`) so methods work AFTER `import_state` (cache empty, state in restored storage). Cache is opportunistic.
- `export_state` serialization is currently `serde_json` over `Vec<(Vec<u8>,Vec<u8>)>` — verbose; fine for the foundation, consider a compact codec later.
- `SignatureKeyPair::from_raw(scheme, priv, pub)` reconstructs the signer from the app's device key — its public MUST equal the cert-certified device key (C3-LINKED), else `validate_leaf` rejects. The host test passes ed25519 32-byte seed as `priv`.

## 🔧 REMAINING for M3 (in order, with deps)

**Rust core:**
- [x] `remove_member(gid, member_account_id)` FFI — done (PCS host-tested).
- [ ] Confirm the group create config's `required_capabilities` floor is what the app wants long-term (currently `floor_required_capabilities()` = KVANT_DEVCERT_EXT + Basic cred).

**Native bridge (spike files):**
- [x] `KvantMlsModule.kt` — m3 @ReactMethods marshalling `MlsClient` — done, compiles.
- [x] Regenerate UniFFI Kotlin bindings — done (MlsClient Object in bindings).
- [x] **Stable KEK** `com.kvant.mlskek` (Keystore-wrapped) — done. *(remaining: add to KvantWipeModule wipe path.)*
- [x] **State persistence to file** (`mls-state.bin`, sealed blob) — done. *(remaining: device-verify + optimize per-op rewrite.)*
- [x] Add `com.kvant.mlskek` (Keystore alias + `kvant_mls` prefs) + `mls-state.bin` to `KvantWipeModule` emergency-wipe — done, compiles.

**JS layer (NEW spike files — parallel to senderkey.js, do NOT touch senderkey.js):**
- [x] `app-rn/src/mlsGroups.ts` — thin JS wrapper over the m3 bridge (init/pinAccount/makeKeyPackage/createGroup/addMember/joinFromWelcome/removeMember/encryptMessage/processIncoming), base64 boundary. **tsc clean.**
- [x] **Identity plumbing** — `mlsGroups.init(MlsIdentity)` builds identity_blob via the Rust single-source-of-truth encoder `mls_encode_identity` (host-tested round-trip) then `m3Init`. account_id = canonical nick; device Ed25519 + C3 cert reused from device.ts/devicecert.js. (Caller feeds actual device identity — see wire-up note.)
- [x] **Wire routing** — tag `0x03 = MLS` implemented in `messaging.ts` (approved). One early-return branch in `handleDeliver` (0x01/0x02 fall through unchanged) → `handleMlsIncoming`. Sub-typed frame (`0x00`=msg/commit, `0x01`=welcome). tsc clean.
- [x] **KeyPackage distribution** — implemented, ZERO server change (bundles stored wholesale). `transport.ts` `publicRegistration`/`decodeBundle` carry an opaque `mlsKeyPackage`; guarded publish hook in `_publishAccountOnce` (byte-identical on non-MLS builds); `mlsFetchKeyPackage` + `mlsAddPeer` add-flow (fetch KP → addMember ghost-checked → commit(0x03) to members + welcome+tree(0x03) to joiner). *(TODO: fetchBundle consumes an OPK — dedicated non-consuming KP fetch; single-use KP.)*
- [x] **Startup wire-up** — `mlsWiring.ts`: `getMlsIdentity()` reuses the app's identity (PRIMARY = account Ed25519 + self-cert; LINKED = TODO) → `ensureMlsInit()` (idempotent, best-effort) hooked into `_publishAccountOnce` alongside the 1:1 crypto (before the KP publish). tsc clean.
- [x] **MLS group roster** — `mlsRoster.ts` (NEW, persisted in Keystore `com.kvant.mlsroster`, separate from Sender-Keys `groupStore`): nicks ∈ group + name + gidB64. Populated on create (adder) and on join (welcome → [me, inviter]). Wiped via `RN_KEYCHAIN` clearAll.
- [x] **Visible working group** — `messaging.ts` high-level ops: `mlsCreateGroup(name)` (native group + roster + `ensureGroupChat` → chat list), `mlsAddToGroup(gidHex, peer)` (roster members → `mlsAddPeer` → roster update), `mlsSendToGroup(gidHex, text)` (roster members → `mlsSendMessage` + `appendGroupOutgoing`). Incoming welcome/message land in the same chat sink.
- **UI flag** — DONE (create + send + add-later; MLS groups are TEXT-only):
  - [x] **Create MLS group action** — `GroupCreateScreen` toggle "MLS · постквантовая группа" (shown only when `mlsAvailable()`), beside Sender-Keys create (untouched). On create → `mlsCreateGroup` + pin each selected member (`peerKeyInfo(nick).key`) + `mlsAddToGroup` → open the Chat. tsc clean.
  - [x] **Send fork** (APPROVED, implemented) — one early-return at the top of `sendGroupMessage(gid, text, …)`: `if (await mlsRoster.getEntry(gid)) { await mlsSendToGroup(gid, text); return; }`. MLS groups (only ones ∈ `mlsRoster`) → MLS; every Sender-Keys group falls through unchanged. **Text only.**
  - [x] **Non-text guards (variant B, approved)** — helper `mlsBlocksNonText(gid, feature)`: if `gid ∈ mlsRoster` → `diag(...)` + return true (EXPLICIT no-op, NEVER a misroute into Sender-Keys). Guards at the top of all 8 other group-send fns: `sendGroupMedia`/`sendGroupReaction`/`sendGroupDeleteForAll`/`sendGroupLocation`/`sendGroupContact`/`sendGroupPoll`/`sendGroupVote`/`sendGroupCallInvite`. **→ media/reactions/replies/mentions/polls/votes/location/contact/calls in MLS groups = FUTURE HARDENING, not silently broken.**
  - [x] **Add-member-later UI** — `GroupInfoScreen` MLS-aware branch: when `getGroup(gid)` is undefined but `gid ∈ mlsRoster`, render a simplified MLS info (shield header + member list + add). Add → `peerKeyInfo(p)` pin (`pinAccount`) + `mlsAddToGroup(gid, p)` + roster refresh — mirrors GroupCreateScreen. Roles/edit/invite-code/leave are Sender-Keys-only, deliberately omitted for the foundation. tsc clean. (Create-time member add already worked.)
- **Multidevice** — scope split into 1a (core+identity) and 1b (fan-out); v2 deferred.
  - [x] **1a — CORE + LINKED identity DONE (2026-08-05; Rust 68/68, tsc clean, KAT byte-identical).** Host test `multidevice_two_leaves_one_account`: two DEVICES of one account (same account_seed→same account_id, different device_seed→distinct leaf keys/certs) are two independent leaves in one tree. PROVES the verified spike already supports it: (1) `member_account_ids` DEDUPS → roster shows the account once (not per-device); (2) BOTH devices decrypt one app message independently (each its own ratchet copy); (3) ghost-defense validates each account-signed leaf on add + commit-consistency. `mlsWiring.getMlsIdentity` LINKED branch reuses this device's per-device 1:1 key (device.ts deviceKey) as the MLS leaf (already per-device, already has an account-signed cert for that exact key; same account_id/nick as primary → two leaves of one account). `toU8` normalizes the persisted cert byte fields. device.ts untouched (read-only). **v1-COMPROMISE (documented): LINKED reuses its 1:1 device key as the MLS leaf** — a DEDICATED linked MLS leaf needs the primary to sign a fresh cert at pairing time = **v2**.
  - [x] **1b-1 — FAN-OUT + SELF-SYNC DONE (2026-08-05; mlsFanout 17/17, tsc clean).** `sendMlsFrame` delivers the (once-group-key-encrypted) frame to EVERY device of each recipient + self-syncs to my own OTHER devices (never the sending device). Pure planner `mlsFanout.planMlsFanout`; reuses `descriptorsOf` (C2 ghost-prune) + per-device `t.send{deviceId,fromDeviceId}`. 🎯 RECEIVE PATH UNTOUCHED — `handleDeliver`/`handleMlsIncoming` are per-device-agnostic (MLS decrypts by leaf state, transport address irrelevant), so the HOT path never changed. Sender-Keys `groupEncryptAndFanOut` byte-identical.
  - [x] **1b-2 — ADD ALL DEVICES + LINKED KP-publish DONE (2026-08-05; mlsFanout 26/26 incl. add-routing, tsc clean, Rust 69/69 untouched).** `mlsAddPeer` adds EVERY device of the peer (descriptorsOf → per-device fetchBundle(deviceId) → N sequential `addMember` via the UNCHANGED guarded_add_members, no batch FFI). Two-pass (fetch KPs, skip none-published → `planAddRouting` drives sends). Per device: commit → existing members (1b-1 fan-out) + peer devices already added THIS sequence (device_0 learns device_1 → lock-step epoch); welcome+tree → that device only. `member_account_ids` dedups (1a) → peer once. **LINKED KP-publish FIXED**: MLS init + pool attach moved into `attachMlsKeyPackages()`, called BEFORE the linked/primary fork in `_publishAccountOnce` (a linked device used to `return` before the MLS attach → never published a KP → couldn't be added). Now both publish. **→ MULTIDEVICE v1 WORKS: a member's devices join at add-time, messages fan out to all, roles account-level.**
  - [ ] **1b residual (honest, → v2): self-sync commit RACE** — two of my own devices committing at once can diverge (v1 relies on the primary committing owner-actions). Plus the deferred v2 below.
  - [x] **v2 DEVICE REMOVAL DONE (2026-08-05; Rust 73/73, mlsFanout 30/30, tsc clean, KAT identical).** Remove ONE device leaf (not the whole account). **Stage A (Rust core):** `remove_device(gid, account_id, device_id)` filters the leaf by (account_id + cert.device_id) → `remove_members` (UNCHANGED, no ghost-guard) → leaf-level PCS (removed device can't read the new epoch; the account's OTHER devices untouched; last device gone → account dropped). `add_revocation(...)` registers an account-signed revocation into the MlsClient TrustStore (mirrors pin_account) — closes the re-add gap (validate_leaf now refuses a revoked device). `member_devices(gid)` read-only FFI = one (account_id, cert.device_id) per LEAF (the correct remove target; for a primary that's the dedicated mlsLeaf fingerprint, ≠ the 1:1 getDevices id). Host: device_removal_pcs, revoked_device_not_readmittable (gap shown then closed), member_devices_lists_each_leaf, remove_device_unknown_is_error. **Stage B (app):** `mlsRemoveDevice(gidHex, nick, deviceIdHex)` authz = SELF or ADMIN (canRemoveDevice, member✗ fail-closed) → remove_device → 1b fan-out → roster sync → if MY OWN device: mint+broadcast account-signed revocation (existing t:revoke channel) → each member add_revocation to their MLS TrustStore (never re-added). Receive: the 1:1 t:revoke handler also feeds MLS TrustStore (one line, 1:1 control path — NOT 0x03/Sender-Keys). UI: GroupInfo long-press → manage devices → per-device remove. authz tested 30/30. 🔴 RESIDUAL (documented): admin removing ANOTHER account's device = leaf-remove/PCS only, NO cert-revocation (admin has no account key) → that device could re-publish a KP; full re-add protection (leaf-remove + revocation) works only for MY OWN device (the main "seized/compromised device" case). Persistence: revocations re-applied to MLS ts on receive; across-restart re-supply is a minor follow-up.
  - **late-join** a NEW device into EXISTING account groups — split A (mechanism) / B (orchestration).
    - [x] **A — MECHANISM DONE (2026-08-05; Rust 74/74, tsc clean, no new FFI).** `mlsLateJoinDevice(gidHex, newDeviceId)`: the PRIMARY adds its OWN new device leaf into ONE existing group (reuses addMember, ghost-checked vs self-pin; idempotent via member_devices; commit → members + my devices, welcome → new device). Host `late_join_own_device`: after a group exists, alice adds her 2nd device post-hoc → it joins + reads (STOP-1); member_account_ids dedups alice (STOP-2); bob untouched (STOP-3). Rust add/ghost core unchanged.
    - [x] **B — ORCHESTRATION DONE (2026-08-05; mlsLateJoin 24/24, tsc clean, Rust 74/74 untouched, KAT identical).** Pure orchestrator `mlsLateJoin.js` (`runLateJoinPass`: sequential iterate, 1 retry per group with `processPending` between attempts, partial-failure isolation, `{ok, failed[reason]}` report) + pairing-completion hook registry (`onDeviceLinked`/`notifyDeviceLinked` — pairing.ts stays free of a messaging import). Wiring: `messaging.mlsLateJoinAllGroups(deviceId)` (mechanism = mlsLateJoinDevice; processPending = yield so concurrently-arrived 0x03 commits apply before the re-add; 5s grace before the pass for the new device's KP-publish), registered at module init; `primaryPairOverRelay` fires the hook fire-and-forget. Host: happy path / epoch-conflict retry / retry exhausted→failed / idempotent re-pass / partial failure / hook-fires. Honest v1 note: Stage A reports failure as boolean-false (native error → diag), so the retry fires on ANY failure — a safe superset of the epoch-conflict retry (mechanism idempotent). 🔴 v2-deferred: polling/offline-primary-catchup (persistent pending-marker), deep commit-queue coordinator for high-activity races.
  - [x] **dedicated linked MLS leaf DONE (2026-08-05; mlslinkedleaf 32/32, tsc clean, Rust 74/74 no diff, KAT identical)** — see the handoff map entry (@baf5a63/@f9329df).
- [x] **Single-use KeyPackage — DONE (2026-08-05; host 69/69, server mls-kp-pool 12/12, tsc clean, KAT byte-identical).** Was: one REUSABLE KP (init_key reused across joins = FS hole + replay). Now: Rust `make_key_packages(n)` = pool of N one-time KPs (each a fresh init_key) + `make_last_resort_key_package()` (reusable LastResortExtension fallback). Client publishes a pool of 10 (`mlsKeyPackages[]`) + last-resort (`mlsKeyPackageLast`); legacy `mlsKeyPackage` kept = last-resort (old-server graceful). Server getBundle pops one unconsumed pool KP per fetch (`consumeMkp`, mirrors OPK pop) → replay bound = one add; drained → last-resort; `clearMkps` on re-register (replenish = full pool republished each launch). `db.js` +consumed_mkps table. KPs opaque base64 (zero-knowledge). Ghost-defense/floor unchanged (kvant_caps declares LastResort SUPPORTED, not required). 🟢 Verified 1:1 X3DH is NOT the same bug (oneTimeCount:32 pool, healthy) — see SECURITY-NOTES. *(minor future: dedicated non-consuming KP fetch so MLS-fetch doesn't also spend an X3DH OPK; server count endpoint instead of full-pool republish.)*
- [x] **Roster-sync hardening — DONE (2026-07-02, host-GREEN 64/64, aarch64 clean, Kotlin compiles, tsc clean).** `member_account_ids(gid)` FFI on MlsClient (reads the TRUE member list from `MlsGroup::members()` — account_ids deduped for multidevice; READ-ONLY, no mutate). Host tests: `member_list_reflects_add_and_remove` + `three_party_roster_consistency` (alice adds carol → **non-adder bob auto-learns via the Add commit** → all THREE agree {alice,bob,carol}; all agree {alice,bob} after remove). Bridge: `m3MemberAccountIds` (no persist) + regenerated uniffi binding + rebuilt .so (arm64+x86_64). JS: `mlsGroups.members(gid)` (account_id bytes→canonical nick), `mlsRoster.setMembers(gidHex, members)`, `syncMlsRoster()` in messaging.ts wired into BOTH the welcome-join path (joiner derives full list from the whole tree it received, not [me,inviter]) and the `commitMerged` path (non-adder auto-syncs on Add/Remove). Safe: nicks are canonical system-wide (saveAccount → canonNick), so `members()` (canonical) matches the transport form → no misroute; adder's incremental roster already canonical-consistent (left as-is).
- [x] **Media + reactions over MLS — DONE (2026-07-02, host-GREEN 65/65, aarch64 clean, tsc clean).** Key insight: the blob layer (0x02 frames, zero-knowledge blob-store, `_pendingBlobs`/`_pendingKeys` correlation by fileId, `receiveBlobManifest`/`downloadMedia`) is agnostic to HOW the file key is delivered — the MLS parallel only swaps the key-envelope carrier. New in messaging.ts: `MLS_CTRL_TYPES = {media, react}` + `mlsSendEnvelope(gidHex, env)` (0x01+JSON `ctrlPlaintext` → `mlsGroups.encryptMessage` → 0x03 fan-out) + `mlsSendGroupMedia` (file layer + v1/v2 blob routing IDENTICAL to `sendGroupMedia`; only the envelope rides MLS). Receive: `handleMlsIncoming` application branch splits on `plaintext[0] === 0x01` → JSON → dispatch ONLY `MLS_CTRL_TYPES` → delegate to the existing gid-agnostic `handleGroupControl` (media v1/v2 + react handling reused verbatim); unknown control type → fail-closed drop (never shown as text); non-0x01 plaintext → text, unchanged (backward compatible with older spike builds). Guard→fork: `sendGroupMedia` + `sendGroupReaction` (one line each; Sender-Keys bodies untouched). Guards KEPT (honest no-op): location/contact/polls/votes (mechanically identical to react — next slice, after the media round-trip is proven on-device), ~~delete-for-all~~ (UNGUARDED 2026-08-05 — MLS roles landed), group calls (production call path — boundary; the ONLY remaining guard).
- [x] **Location/contact/polls/votes over MLS — DONE (2026-07-02, tsc clean; envelope transport already proven by `media_filekey_roundtrip`).** Mechanically identical to reactions: each send builds the IDENTICAL envelope once and forks the carrier (MLS `mlsSendEnvelope` vs sender-key `groupEncryptAndFanOut` — Sender-Keys wire byte-for-byte unchanged); `MLS_CTRL_TYPES` extended to {media, react, location, contact, poll, vote}. Receive branches verified gid-agnostic before unguarding (appendGroupIncomingData / applyPollVote — chat-keyed by gidHex, no groupStore/roles/sender-key state). Still guarded (honest no-op): **delete-for-all** (authz = `isAdmin`, Sender-Keys roles — needs MLS roles first) and **group calls** (production call path — boundary). No new host test needed: these ride the exact envelope mechanism `media_filekey_roundtrip` proves; the per-type receive logic is the same code Sender-Keys groups already exercise.
- [x] **Leaf-key hardening — DONE (2026-08-05, commits leaf-impl + leaf-tests; Rust 67/67, mlsleaf.test 10/10, tsc clean, host-KAT byte-identical).** PRIMARY device now uses a DEDICATED per-device MLS leaf Ed25519 (`mlsLeaf.ts`, Keystore `com.kvant.mlsleaf`, generate-once via `generateDeviceKey`) instead of the account key. The account ATTESTS it via a normal C3-LINKED device cert (`getMlsIdentity`: account_public_key = account pub, device_public_key = leaf pub). **Ghost-defense UNCHANGED, Rust untouched** — `verify_device_bundle` already required leaf sig_key == cert.device_public_key + cert chained to the pinned account; that separate-key model is exactly what `client/tests::mk` (account_seed ≠ device_seed) has always verified. New Rust anchor `primary_model_dedicated_leaf_attested_and_swap_rejected` (positive add+join, negative leaf-swap fail-closed). Wipe: `com.kvant.mlsleaf` in JS belt + `wipeMlsLeaf` (native clearAll already covers it). First per-device init supersedes any v1 account-leaf state (M3 undeployed → no groups). Roles/anchor/KAT untouched (genesis stays account-signed; KAT = ml-kem, not the leaf path). LINKED-device leaf still multidevice-TODO.
- [x] **MLS-group ROLES + delete-for-all — DONE (2026-08-05, commits c92d535/…roles core…/7785fae/f36c87f; mlsroles 29/29, cargo 66/66, tsc clean).** Sender-Keys role model (P0-1 genesis + P1 owner-signed hash chain) carried to MLS: owner = creator (immutable, genesis first-write-wins), roles change ONLY via owner-signed chain entries (grouplog.js REUSED AS-IS; new pure primitive crypto-core/mlsroles.js + vendored copy). **Genesis verification ANCHOR = the MLS tree itself**: new read-only FFI `member_account_keys` returns each account's Ed25519 pub from the ghost-defense-VALIDATED leaf credentials (== pinned, cert-verified) → a member cannot forge the anchor; the key is pinned WITH the genesis so roles survive the owner leaving (frozen, no new grants). Transport = `t:'groles'` MLS envelope (relay-safe, any member may rebroadcast; adder rebroadcasts after each add so joiners learn roles). delete-for-all UNGUARDED: `t:'del'` over MLS, receive-side author-or-admin per the VERIFIED chain (fail-closed: no pinned roles → author only); handled directly in handleMlsIncoming so the Sender-Keys del branch is byte-identical. UI: role badges + owner grant/revoke (setMlsAdmin), add-member gated by admin/owner, ChatScreen del menu MLS-aware. 🔴 HONEST RESIDUAL (accepted v1): membership COMMITS are NOT role-enforced in the Rust receive path (an insider can Add around the UI gate; ghost-defense still guarantees the added member is a genuine certified account, never a ghost) — deterministic commit-authz needs roles in group state (extension = the deferred Option A, blocked on the extensions-draft Contract-2 guard). gmreq-parity (admin asks, owner serializes) = later.

### ✅ Wire-routing (tag 0x03) — IMPLEMENTED, surgical, Sender-Keys untouched
Fork point: `messaging.ts` `handleDeliver` (line ~620, `const tag = wire[0]`). Currently: `0x02`→blob, else→ratchet/Sender-Keys.
- Add `const TAG_MLS = 0x03;` next to TAG_CONTROL/TAG_BLOB.
- Add ONE early branch **after** the existing block-check + diag, **before** the TAG_BLOB check:
  `if (tag === TAG_MLS) { await handleMlsIncoming(d.from, wire.slice(1)); return; }`
  → fires ONLY for 0x03; 0x01/0x02 fall through byte-for-byte unchanged (the current app never emits 0x03, so Sender-Keys traffic can't hit it).
- 0x03 frame format: `0x03 || u16be(len(gid)) || gid || mls_wire`. `handleMlsIncoming` parses gid, calls `mlsGroups.processIncoming(gid, mls_wire)`; on `application` → surface plaintext to the group chat (same sink as Sender-Keys group msgs); on commit/proposal → no UI (native membership change). Guard with `mlsAvailable()` (drop+diag if not built).
- SEND: helper `mlsFrame(gid, wire)` = `0x03 || u16(gid.len) || gid || wire`, sent over the SAME opaque transport `t.send(...)` Sender-Keys uses.

### KeyPackage distribution plan (step after routing)
- Publish our `makeKeyPackage()` blob to the server keyed by (nick, deviceId); fetch a peer's before `addMember`. Rides the existing bundle/prekey mechanism (server stores it as an OPAQUE blob → zero-knowledge preserved), or a small `publishKeyPackage`/`fetchKeyPackage` transport call.
- Add flow: fetch peer KP → `addMember(gid, kp)` → broadcast `commit` (0x03) to existing members + send `welcome`+`ratchetTree` (0x03) to the new member.
- Single-use KP (like OPKs) is the hardened target; a reusable last-resort KP is the simplest start.

**UI (behind a flag; last):**
- [ ] Create-group → optional MLS path (flag). Send/receive over MLS. No visual change to Sender-Keys groups.

## 🔴 Boundaries (unchanged)
- **DO NOT touch**: `senderkey.js`, production `messaging.ts`/crypto/call, the live Sender-Keys path.
- M3 = a **parallel** MLS path; Sender-Keys keeps working. Migration = **M5**.
- Everything untracked (spike). Each piece → host-test first (like M1/M2/this).

## Related / residual
- **M2 device test**: APK on Desktop (`kvant-MLS-M2-spike-arm64.apk`), ADB run pending.
- **libcrux bump — ✅ DONE on host (Route 2, 2026-08-05)**: `openmls 0.9.0-rc.1` family +
  `openmls_libcrux_crypto 0.4.0-rc.1` (crates.io), all 4 HIGH closed (`cargo audit` clean), host-KAT
  byte-identical on ml-kem 0.0.10, 65/65 green, both ABIs rebuilt + jniLibs refreshed. See
  SECURITY-NOTES.md "ROUTE 2 EXECUTED". REMAINING: device-KAT on both SoC (device session), fuzz-crate
  bump before Tier-2, rc.1→final 0.9.0 when released.
