// Proc-macro uniffi mode (setup_scaffolding! in lib.rs) needs no UDL scaffolding step. Kotlin
// bindings are generated library-mode from the built .so (see scripts/build-mls-aar.ps1). No-op.
fn main() {}
